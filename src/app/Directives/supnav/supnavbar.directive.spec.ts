import { SupnavbarDirective } from './supnavbar.directive';

describe('SupnavbarDirective', () => {
  it('should create an instance', () => {
    const directive = new SupnavbarDirective();
    expect(directive).toBeTruthy();
  });
});
