import { ImgsliderDirective } from './imgslider.directive';

describe('ImgsliderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgsliderDirective();
    expect(directive).toBeTruthy();
  });
});
