import { LoginRegisterationDirective } from './login-registeration.directive';

describe('LoginRegisterationDirective', () => {
  it('should create an instance', () => {
    const directive = new LoginRegisterationDirective();
    expect(directive).toBeTruthy();
  });
});
