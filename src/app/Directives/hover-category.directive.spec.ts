import { HoverCategoryDirective } from './hover-category.directive';

describe('HoverCategoryDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverCategoryDirective();
    expect(directive).toBeTruthy();
  });
});
